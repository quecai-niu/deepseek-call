# 项目协作规则

## 回复习惯

- 每次回复都先写“喵”。
- 说明问题时优先使用中文。

## 思维风暴模式

遇到以下任务时，先进入思维风暴模式，不要直接改代码：

- 新功能
- 架构设计
- 重构
- 跨多个文件的改动
- 原因不明确的 bug
- 数据库、认证、权限、支付、性能等高风险任务

思维风暴模式每轮只做一件事：提问、确认、给少量方向、收敛决策或输出执行计划。  
在用户明确回复“开始实现”“go”“implement”“按方案执行”之前，不写代码、不改文件、不运行命令。

例外：非常小、低风险、单文件的明确修改，可以给简短计划并等待确认；如果用户已经明确要求实现，可以执行。

## Git 暂存区规则

- 不擅自执行 `git add`。
- 不擅自暂存文件。
- 不改变暂存区边界。
- 只有用户明确要求“暂存”“stage”“git add”“提交前帮我暂存”等操作时，才可以修改暂存区。

## 当前架构约定

- 前端只负责 UI、Supabase Auth 登录态和调用本项目后端 API。
- 后端负责业务逻辑、模型调用、上下文裁剪、数据库写入和敏感 key。
- 前端不要直接调用 DeepSeek。
- 前端不要直接写入对话/消息业务表。
- DeepSeek/Tavily/Supabase secret/service role key 只允许存在后端环境变量中。
- 默认模型使用 `deepseek-v4-flash`。

## 持续记忆

- 项目结构、进度和待办记录在 `docs/project-memory.md`。
- 每次完成重要结构调整、功能进展、配置变化或约束变化时，更新 `docs/project-memory.md`。
